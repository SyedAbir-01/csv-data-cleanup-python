# CSV Data Cleanup Automation

A reusable Python tool for cleaning CSV files automatically.

## Features

- Removes duplicate rows
- Removes completely empty rows
- Normalizes unnecessary whitespace
- Optional lowercase conversion
- Preserves the CSV header
- Displays cleaning statistics
- Generates an optional cleaning report
- Works with different input and output files

## Example

### Input

```text
customers.csv
